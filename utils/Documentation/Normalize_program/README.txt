Steps To execute the code:
    1.Copy the selected yaml files which has to be normalised from Master_Folder to input folder 
    2.Run the command sudo python yaml_template.py to execute the program
    3.The normalized file will overwritten or created in the Normalized_Files. 
