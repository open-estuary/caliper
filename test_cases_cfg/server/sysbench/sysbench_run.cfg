[sysbench]
category = Performance application sysbench
scores_way = exp_score_compute 4 -0.5
command = "pushd sysbench; ./sysbench.sh root root; popd"
parser = sysbench_parser
[sysbench_cpu]
category = Performance cpu
scores_way = exp_score_compute 4 -0.5
command = "pushd sysbench;./sysbench_cpu.sh;popd"
parser = sysbench_parser

